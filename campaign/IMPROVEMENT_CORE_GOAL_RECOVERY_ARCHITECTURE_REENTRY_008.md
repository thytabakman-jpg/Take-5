# Improvement Core Goal Recovery and Architecture Reentry 008

Date: 2026-09-24
Status: EXECUTED GOAL RECOVERY / REENTRY
Migration authority: NONE

## Recovered governing goal

Build and validate the strongest supportable nonproduction successor foundation for the research system so that arbitrary research/project material can enter, the system can autonomously orient/discover/select/execute/verify/reenter, preserve and accumulate knowledge/capabilities without silent semantic or authority drift, improve its own tools/processes under the same governance law, and recover all demonstrated historical functionality.

Then produce a readiness packet and STOP before migration. Migration requires explicit user authorization after a separate all-tools readiness attack.

This supersedes local goals such as:
- perfecting Semantic Governance Admission;
- perfecting MTA;
- hardening every tool before architecture;
- fitting discoveries into the current Take-5 shape.

Those are means/subproblems.

## Current architectural constraint recovered from latest work

Existing system basis remains:

T=<K,S,O,G,M,C,R,U>.

New protected transition law:

AuthoritativeEvolution(S0)=Paths(->_K,S0)

where every authoritative effect is realized only through K-constrained, C-admitted, effect-scoped U transitions with evidence/provenance, authority, preservation obligations and explicit unresolved status.

This is a constraint on candidate architectures, not a ninth role.

## Actual live frontier

The previous side investigation is sufficiently closed. The bottleneck has returned to architecture synthesis.

The next job is not another isolated MTA pass.

It is:

Construct the smallest high-ceiling candidate foundation architecture that simultaneously realizes:
1 historical protected functionality;
2 eight-role mathematics;
3 authoritative-transition law;
4 artifact-reality/persistence obligations;
5 controller/worker separation;
6 durable event history + derived state;
7 candidate/evidence/OPEN versus authoritative state;
8 capability registry/router and adaptive reentry;
9 semantic-worker extensibility;
10 self-improvement without self-promotion;
11 zero-request and directed intake;
12 execution/verification receipts;
13 map/anti-loss capture;
14 external-action boundary;
15 explicit genesis/bootstrap;
16 nonproduction testability.

## Design order

Do not start from modules or old Take labels.

1 Semantics:
define state strata, proposal/effect, witness/provenance, C, U, verification, authority, reentry, genesis, external action.

2 Behavioral obligations:
map historical CAP-001..033, C01-C49, composites and recovered machinery onto those semantics.

3 Architecture:
derive the minimal components/interfaces that realize the semantics and obligations.

4 Adversarial analysis:
MTA, Multi-Object, Architecture Analysis, Diagnosis, PD/PDAudit, RTC/MTOS, ARA/Orphan/Ghost.

5 Build:
implement nonproduction successor.

6 Validate:
matched regressions, zero-request, messy-folder, broad-corpus, delegation, order, unfamiliar holdout, artifact reality.

7 Readiness packet.

8 STOP BEFORE MIGRATION.

## Improvement Core decision

REENTER_BIG_PROJECT = YES.

Immediate next action:
architecture synthesis from semantics + protected behavior, with no presumption that the existing Take-5 module layout is correct.

Completion of the next architecture-synthesis episode requires:
- one explicit candidate architecture;
- mapping from each foundational semantic role to implementation responsibility;
- mapping from protected historical behavior to owner/path;
- explicit bypass/authority boundaries;
- explicit OPENs;
- attacks showing why the architecture is smaller/higher-ceiling than recovered predecessors.

No migration or production promotion.
