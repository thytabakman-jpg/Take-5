# Improvement Core — MTA Target Selection 001

Date: 2026-09-24
Controller: Improvement Core
Status: EXECUTED TARGET SELECTION / NEXT ACTIONS BOUND

## Goal

Select the current objects where MTA's job — structurally understand, diagnose, reduce, reconstruct, and validate a mathematical/tool object relative to a frozen job — has high expected discriminating yield.

MTA is not applied ceremonially. Selection requires a live structural uncertainty that can change foundation design, implementation, or validation.

## Priority 1 — Artifact Reality Protection primitive

Target:
Take-5 research/ARTIFACT_REALITY_FOUNDATION_CANDIDATE_001.md

Why now:
The latest ARA repair generated a new candidate foundational invariant and a placement split:
Kernel candidate = artifact-reality protection contract + transition legality.
Core/Foundation candidate = contract + discovery/repair/verification machinery.
Runtime = scanners/writers/executors.

Live MTA questions:
- What is the minimal mathematical object?
- Which coordinates are invariant, judgment, state, transition guard, repair operator, or verification?
- Is ArtifactDelta primitive or derived?
- Is CAC a predicate, closure judgment, transition guard, or package?
- Which parts factor through existing identity/currentness/authority/preservation mathematics?
- Does this create a genuinely new primitive or a composition of maps 101, 118-123 and ARA?
- What survives ablation when repository-specific machinery is removed?

Expected effect:
DIRECT on Core/Kernel PD and foundation architecture.

## Priority 2 — CORE PD object

Target:
Take-5 research/PD_CORE_001.md

Why now:
Core is provisionally defined as a minimal reconstructive basis relative to protected behavior, but three rival representations remain live:
minimal role set;
minimal behavioral-factor set;
typed generating basis over roles.

MTA can factor the definition, expose hidden primitives in Reconstructs/Minimal/admissible reduction/protected behavior, and test whether system core is a set, quotient, generating basis, or configuration-relative equivalence class.

Expected effect:
DIRECT. Architecture cannot be frozen while Core's mathematical type remains ambiguous.

## Priority 3 — KERNEL PD object

Target:
Take-5 research/PD_KERNEL_001.md

Why now:
Kernel currently mixes AlwaysOn, Privileged, identity/legality preservation and minimality. Historical K, distributed-kernel maps, execution kernel, and microkernel evidence may be different mathematical objects.

MTA questions:
- Is kernel a subset of core, projection, privileged subgraph, closure operator, invariant set, or transition guard?
- Are AlwaysOn and Privileged independent?
- Does transition legality require executable mechanism in kernel or only a contract?
- Which K coordinates are configuration rather than kernel?
- Can kernel minimality be stated without circular dependence on the system whose legality it defines?

Expected effect:
DIRECT and high leverage.

## Priority 4 — Project Mathematics canonical reconstruction + CAC

Targets:
Reaserch PROJECT_MATHEMATICS_CANONICAL_RECONSTRUCTION_001 and PROJECT_MATHEMATICS_CAC_LEDGER_001.

Why now:
ARA repaired artifact reality, but the repair introduced a nine-layer typed composition PM=SEM⊕SYS⊕TOOL⊕SEARCH⊕REL⊕RTC⊕CTRL⊕EXEC⊕LIFE and a CAC verification object. These are fresh mathematical candidates and have not received MTA.

MTA questions:
- Is ⊕ well typed?
- Are the nine layers independent, overlapping, or projections?
- What is the reconstruction relation?
- Is family-level CAC sufficient for project-level reconstruction?
- Is ArtifactDelta empty relative to a declared dependency basis or globally?
- What is the minimal compliance witness?

Expected effect:
DIRECT on artifact-reality guard and anti-loss architecture; SUPPORT on broader foundation.

## Priority 5 — Take-5 foundation architecture after 1-4

Target:
ARCHITECTURE_CANDIDATE.yaml plus the revised Core/Kernel/ARA results.

Why not first:
MTA on the whole architecture before the three foundational objects are retyped would mostly rediscover their ambiguity.

Trigger:
Run after material deltas from priorities 1-4 stabilize.

Expected effect:
DIRECT architecture synthesis.

## Priority 6 — Take-5 dump/runtime state model

Targets:
runtime/take5_dump.py, worker adapter contract, external architecture finding EventHistory != DerivedState != LearnedExperience.

Why later:
Current runner is explicitly a reference fixture. MTA becomes high-yield once state/event/worker contracts are represented mathematically rather than as provisional code.

Questions:
minimal continuation-sufficient state; event-log quotient; replay semantics; worker/controller boundary; execution-truth carrier; failure memory; reentry fixed point.

Expected effect:
DIRECT on runnable build.

## Do not apply MTA now

- every historical map individually: low yield until foundation dependencies identify which are load-bearing;
- solved ARA repair mechanics as repository scripts: implementation detail unless they expose a mathematical primitive;
- external papers individually: use them as evidence/challenges to the target objects;
- migration mathematics: migration is outside active target;
- current deterministic fixture merely because it exists: validate behavior first; MTA its state/control mathematics when formalized.

## Selected sequence

MTA(ArtifactReality)
-> reassess
-> MTA(Core)
-> reassess
-> MTA(Kernel)
-> reassess
-> MTA(ProjectMath/CAC)
-> reassess
-> MTA(revised foundation architecture)
-> reassess
-> MTA(runtime/state model when formalized).

Material delta at any step can reorder the remaining frontier.

## Verdict

The highest-yield immediate MTA target is Artifact Reality Protection because it is both newly discovered and potentially kernel/core load-bearing.

Core and Kernel are next because their unresolved mathematical types directly control architecture placement.

Do not begin with a blanket MTA sweep of every map.
