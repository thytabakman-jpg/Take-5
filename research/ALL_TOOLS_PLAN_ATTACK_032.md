# All-Tools Plan Attack 032

Date 2026-09-24
Target: SIX_BY_SIX_BY_SIX_FOUNDATION_031 and migration plan
Controller: ImproveCore
Disposition: PLAN REVISED BEFORE PROMOTION

## Frozen job

Find the strongest supportable migration foundation without losing demonstrated behavior.
Do not canonize a visually attractive 6x6x6 factorization merely because several six-structures exist.

## Tool-stack convergence

MT/MTA:
The first 6x6 is supported as directed source-scope x target-scope geometry.
The proposed third six in 031 was underdetermined.

PD/Differentiate:
At least four distinct six-valued structures must not be conflated:
1 scope ontology S = {SYS,SUB,COMP,INT,BND,XL}
2 transition-kind labels T = {SELF,PROJECT,LIFT,RELATE,REDECOMPOSE,TRANSPORT}
3 mode faces F = {EXPAND,CONTRACT,INWARD,OUTWARD,ISOLATE,COUPLE}
4 IOOC axes I = {containment,functional,reality_plane,representation,environment,epistemic_meta}
A fifth candidate bridge basis has six provisional kinds, with RECLASSIFY reducibility still OPEN.

Factor:
Mode faces are not a six-valued independent coordinate. They are poles of three binary axes.
A complete mode has 2x2x2=8 corners.
Therefore S_src x S_dst x F_mode is useful as a face-indexed audit surface, but is not yet justified as the canonical third factor of run identity.

Architecture:
The correct foundation must separate:
semantic algebra T=<K,S,O,G,M,C,R,U>;
scope graph SxS;
transition/bridge semantics;
mode cube;
IOOC traversal axes;
representation/view/candidate-universe feedback;
configured-run/activation/evidence;
authority/currentness/closure.

Root Cause:
The recurrent generator is COORDINATE CONFLATION UNDER SUCCESSIVE DISCOVERY.
A newly discovered useful six-set gets promoted into the same architectural role as an earlier six-set before independence, typing, and consumer effect are established.

Six-scope Root Cause:
SYSTEM: foundation mixes geometry, control, traversal and bridge roles.
SUBSYSTEM: tool/controller/analysis subsystems consume different coordinates.
COMPONENT: local six-sets can be valid without being global axes.
INTERFACE: missing typed adapter between scope edge, mode policy and IOOC traversal.
BOUNDARY: unclear split between run identity, audit lens and transition semantics.
CROSS_LAYER: semantic six-sets were promoted into runtime architecture before execution evidence.

Multi-Object:
SxS, Mode, IOOC and bridge algebra have irreducible interactions, but no evidence yet licenses collapsing them into one 6x6x6 cube.

Goal Spine:
Protected goal is migration without loss, not numerical elegance.
Any coordinate is admitted only if removing it changes protected discovery, legality, execution, verification or closure.

RTC:
Strict successor to 031 is a FACTORED COORDINATE ARCHITECTURE, not a larger blind tensor.

TransferCore:
Transfer each discovered six-structure by semantic role, not by cardinality.
Same number of members is not a transfer license.

IOOC:
All six IOOC axes and all 15 axis-pairs were material in prior campaign. This makes IOOC a serious rival for the supposed third-six role and proves 031 chose Mode prematurely.

Candidate Universe Auditor:
Third-factor candidate universe is incomplete unless it contains at least Mode, IOOC, transition-kind and bridge-basis rivals.

Basis Change Revalidator:
031 cannot be promoted until the rival-factor discrimination is complete.

Runtime Binder:
Runtime should carry typed coordinates separately and only materialize those required by a run. Do not allocate 216 canonical runtime states.

Selector Calibrator:
Selection target is family x scope-edge x mode plus optional traversal/challenge coordinates, not one flattened integer cell.

Cross-Map Residual:
Representation/view changes can alter which coordinate becomes material. Preserve dynamic coordinate activation.

Holdout Forge:
Required discriminator is coordinate ablation on matched tasks.

Tool Interaction:
Test ScopeEdge x Mode, ScopeEdge x IOOC, Mode x IOOC, and three-way residuals before choosing a canonical product.

Semantic Drift:
Guard against six-by-cardinality equivalence.

Configured Run / TRC:
Every admitted coordinate requires semantic identity, execution envelope, closure/reentry and consumer path.

HF-001:
Material coordinate delta reopens architecture basis.

## Corrected architecture candidate

Foundation is factored, sparse and typed:

F* =
<SemanticAlgebra,
 ScopeGraph,
 TransitionSemantics,
 ModePolicy,
 TraversalAxes,
 RepresentationSystem,
 CandidateUniverse,
 ConfiguredRun,
 EvidenceAuthority,
 ReentryClosure>.

ScopeGraph = S x S, |S x S|=36.

ModePolicy = three binary axes with eight complete modes and six face projections.

TraversalAxes = IOOC six-axis family, activated when result-sensitive.

TransitionSemantics = typed edge/bridge law, not assumed to be a third six dimension.

A run is sparse:
Run = <Family, Edge, Mode, OptionalTraversalPackage, Contract, State, Views, Evidence, Authority>.

## Kernel consequence

Kernel should enforce typed-coordinate identity and anti-conflation, not hard-code 216 states.

New guard:
SameCardinality(A,B) does not imply SameRole(A,B) or ProductFactor(A,B).

Coordinate admission requires:
DistinctRole AND ResultSensitive AND ConsumerEffect AND NonreconstructibleFromExistingCoordinates.

## Plan

P1 inventory every current coordinate/family and type its role.
P2 run reduction/ablation for rival six-structures.
P3 run interaction matrix and higher-order residual only where witnessed.
P4 admit minimal coordinate basis.
P5 bind sparse runtime representation.
P6 integrate RCDL and scope/mode-sensitive reentry.
P7 replay Take-2/3/4 and current historical benchmarks.
P8 unlike-domain holdout.
P9 currentness + regression + rollback + migration gate.

## Decision

031 is retained as a useful hypothesis/audit surface but is NOT the canonical foundation.
Migration remains NO-GO during this discrimination.
The plan is stronger because it is designed to survive the next six-like discovery without another foundation rewrite.
