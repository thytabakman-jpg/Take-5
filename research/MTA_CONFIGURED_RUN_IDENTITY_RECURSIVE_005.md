# Recursive MTA on Configured-Run Identity 005

Date: 2026-09-24
Status: EXECUTED SEMANTIC RECURSION / BASIS-RELATIVE
Authority: research only
Target:
Take-5 research/IMPROVECORE_RECURSIVE_WRAPPER_REPAIR_001.md
and research/RECURSIVE_GDOS_RERUN_001.md

## Configured execution

Each MTA round is treated as:
MTA round -> Tool Run Closure -> HF-001 reentry.
A failure-mode-distinct challenge is used after same-basis stability.

## Round 1 — factor the wrapper claim

Prior candidate:
ConfiguredRunSpec =
<SemanticNucleus, EpisodeEnvelope, RecursionOwner, ConsequenceScope,
 ClosureContract, ReentryContract, ExternalEscapeContract, TerminalContract,
 PersistenceContract>.

MTA finds four logically different layers hidden inside this tuple:

1. Tool identity
   <Job, SemanticNucleus, ProtectedBehavior>.

2. Run policy
   <Envelope, RecursionOwner, ReentryTrigger, ExternalEscape>.

3. Effect closure
   <ConsequenceScope, Persistence, ConsumerTrace, Recovery>.

4. Claim closure
   <TerminalContract, CoverageBasis, ChallengeBasis>.

Material delta:
YES.
A tool can preserve its semantic identity while changing run policy, effect closure, or claim strength.

TRC:
ACCOUNTED.

HF-001:
REENTER.

## Round 2 — attack "maximized tool"

"Maxed out" is not one predicate.

Define maturity vector:

Maturity(T) =
<Identity,
 MathematicalReduction,
 RecursiveSelfApplication,
 ExternalChallenge,
 ConfiguredRun,
 PersistenceClosure,
 RuntimeRealization,
 HoldoutEvidence,
 CanonicalCurrentness,
 CrossToolRevalidation>.

Each coordinate has states:
PASS
PARTIAL
OPEN
NOT_APPLICABLE
STALE.

A tool is MAXIMIZED_RELATIVE(B) only if every result-sensitive coordinate needed by its job is PASS or typed OPEN with a known discriminator, and no admitted same-job strict successor remains under basis B.

Material delta:
YES.

TRC:
ACCOUNTED.

HF-001:
REENTER.

## Round 3 — cross-time attack

A historical maximization certificate can become stale when shared mathematics changes.

If tool T imports shared basis B_t and B_t -> B_(t+1) materially changes a result-sensitive coordinate, then:

Maximized_Bt(T)
does not imply
Maximized_B(t+1)(T).

Therefore every tool needs a currentness/revalidation dependency on:
- shared mathematical kernel;
- HF-001 current semantics;
- Tool Run Closure current semantics;
- execution-truth model;
- capability/program/controller hierarchy;
- any tool-specific imported basis.

Material delta:
YES.

TRC:
ACCOUNTED.

HF-001:
REENTER.

## Round 4 — portfolio consequence

Portfolio completeness requires more than tool-by-tool maturity.

Let ToolSet B be current named tools.

PortfolioClosed(B) requires:
1. every material tool identity is recovered;
2. aliases/supersessions are resolved;
3. each tool has maturity vector;
4. every stale tool has a revalidation disposition;
5. interactions/packages with demonstrated result sensitivity are audited;
6. missing expected tools/programs are detectable;
7. current invocation resolves to canonical configured semantics.

This introduces a distinct object:
TOOL MATURITY / CURRENTNESS MATRIX.

Material delta:
YES.

TRC:
ACCOUNTED.

HF-001:
REENTER.

## Round 5 — primitive vs composite attack

"Run MTA on every tool" must not imply every named tool is a primitive.

For each named T, MTA must return one of:

PRIMITIVE_CANDIDATE
SHARED_KERNEL_SPECIALIZATION
COMPOSITE_PROGRAM
CONTROLLER
CONFIGURED_REGIME
ALIAS_VIEW
HISTORICAL_ONLY
OPEN_IDENTITY.

A tool that reduces fully to existing components may still be valuable as a configured program.

Material delta:
YES.

TRC:
ACCOUNTED.

HF-001:
REENTER.

## Round 6 — maturity and maximization are different

A mathematically reduced tool can be immature operationally.
A runtime-realized tool can be mathematically stale.
A recursively self-stable tool can lack external challenge.
A canonical tool can be unvalidated empirically.

Therefore no scalar "tool quality" is licensed.

Use partial vector status.

Material delta:
YES.

TRC:
ACCOUNTED.

HF-001:
REENTER.

## Round 7 — zero-new-coordinate check

All further variants reduce to:
identity,
reduction,
recursive self-application,
external challenge,
 configured run,
 effect/persistence closure,
 runtime,
 holdout,
 canonical currentness,
 cross-tool/current-basis revalidation.

Material delta:
ZERO.

Same-basis relative close candidate.

## External challenge — Orphan/Ghost + Architecture

Challenge:
Can the matrix miss a tool that was expected but never represented?

Finding:
YES.
A portfolio audit based only on observed named tools inherits observed-object bias.

Required addition:
ExpectationCoverage.

Tool maturity matrix must be generated from both:
A. recovered actual tool objects;
B. expected jobs/capabilities/programs implied by current architecture and historical protected behavior.

Missing expected object => TOOL_ORPHAN candidate.
Existing named object with no valid current job/ground => TOOL_GHOST candidate.

Material delta:
YES.

HF-001:
REENTER.

## Round 8 — integrate expectation coverage

Final candidate maturity object:

ToolMaturity(T) =
<Identity,
 ExpectationGround,
 MathematicalReduction,
 RecursiveSelfApplication,
 ExternalChallenge,
 ConfiguredRun,
 PersistenceClosure,
 RuntimeRealization,
 HoldoutEvidence,
 CanonicalCurrentness,
 CrossToolRevalidation>.

Portfolio state additionally tracks:
ToolOrphans,
ToolGhosts,
Aliases,
Supersessions,
InteractionPackages,
CoverageOpen.

Material delta:
ZERO after integration.

## Round 9 — confirmation

No new maturity coordinate appears.
Further questions concern measurement and evidence, not new structure.

Material delta:
ZERO.

## Disposition

CONFIGURED_RUN_IDENTITY_MTA:
MATERIAL YIELD -> EXTERNAL REOPEN -> BASIS-RELATIVE STABILITY.

Main correction:
"Has this tool been maxed?" is an 11-coordinate maturity question, not a yes/no label.

A previous maximization result can become STALE when shared mathematics or configured-run semantics changes.

Therefore the portfolio requires a full currentness-aware MTA sweep now.
