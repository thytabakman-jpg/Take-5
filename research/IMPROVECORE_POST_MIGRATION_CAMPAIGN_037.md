# ImproveCore Post-Migration Campaign 037

Date 2026-09-24
Status EXECUTED DECISION / ACTIVE MAINTENANCE BASIS

## Rewritten governing prompt

Given Take-5 as canonical working system, autonomously:
1 classify every registered capability by whether the new host architecture is actually consumed and whether its semantic nucleus is current;
2 repair backlog/debt/workstream/transfer handling as typed state/views rather than duplicated queue primitives;
3 decide whether to mutate the current kernel, create a new successor core, or use a clean-room experimental successor;
4 route paused projects only when the host-system problem that blocked them is actually repaired;
5 recover current PD, canonical-authority, project-priority, and BindingTopology state;
6 prevent unbounded self-improvement by requiring material delta, admission, budget and relative closure.

Do not upgrade ceremonially. Upgrade only on result-sensitive deficiency.

## 1 Tool upgrade decision

The migration upgraded the host, not every semantic nucleus.

Current executable registry audit:
C01-C49 are runtime-bound.
CAP-001..CAP-033 are historical reconstruction witnesses, not active capability programs and must not be routed as if they were C capabilities.

The old root-cause document claiming C20-C48 unbound is historical/stale relative to current a5_programs.py and must not govern currentness.

Required maturity vector for each active C capability:
M(t)=<TypedIdentity,SemanticCurrentness,HostConsumption,ConfiguredRun,ScopeModeCoverage,ExecutionReceipt,Reentry,HoldoutOrFixture,Currentness>.

Disposition:
A CURRENT when all required dimensions pass.
B REVALIDATE when executable but semantic nucleus predates material architecture/math change.
C REPAIR when binding/host-consumption/result-sensitive test is missing.
D SUPERSEDED when behavior reconstructs from admitted host/tool composition with preservation witness.

Action:
run A/B/C/D classification over C01-C49.
Do not treat CAP historical rows as active tools.
B/C become work items automatically.
No global rewrite solely because host changed.

## 2 Backlog repair

Legacy queue policy remains correct in principle:
BACKLOG, DEBT, WORKSTREAM, SORT_LATER, TRANSFER_QUEUE are views over typed Work/Relation/State unless strict gain requires a durable separate surface.

Current runtime is too thin: WorkItem only stores id/obligation/material/licensed/reachable and therefore cannot faithfully encode queue species, provenance, risk/cost, activation authority, target project, or transfer relation.

Repair basis:
WorkItem* =
<id,kind,obligation,status,provenance,authority,material,reachable,cost,risk,target,relations>.

Kinds:
CANDIDATE
DEBT
ACTIVE_WORKSTREAM
UNRESOLVED_ROUTING
TRANSFER

Activation remains an authority-bearing transition.
File order never establishes priority.
OPEN/incomparability survive.
Closure is illegal when a material typed work view has no terminal disposition.

## 3 Successor/core decision

Do NOT replace Take-5 on a calendar.
A two-week forced migration cadence is rejected because elapsed time is not evidence of architectural gain.

Do NOT continuously mutate the canonical kernel for every discovery.
Kernel changes remain admission-gated because kernel failure has system-wide blast radius.

Create an experimental successor lane only when a material architectural delta exists:
Take-6-Lab (working name) =
clean-room/reconstruction test bed, no canonical authority.

Promotion trigger:
StrictGain AND Preservation AND Holdout AND Currentness AND RollbackReady.

Take-2/3/4 remain frozen benchmark/evidence sources, not live runtime dependencies.
Their valuable behavior is reconstructed into the lab when needed.

## 4 Sukkos booklet

The host-system blocker that paused the booklet has been operationally repaired: independent gates/workstreams and constrained production path exist.

Resume only from accepted current state:
Fick ACTIVE REPAIR via WI-FICK-001.
Keva released.
Conservation unresolved bridge.
No broad architecture audit unless a booklet gate fails.

Take-5 can now own constraint persistence and project routing; it does not imply the booklet itself is already finished.

## 5 Canonical authorities

Do not claim all newest Take-5 tools have been run on canonical authorities.
Prior canonical-authority work produced substantial PD/architecture/MT-style analysis, but the post-migration C01-C49 maturity/currentness campaign and newest BindingTopology challenge have not been exhaustively replayed there.

Action after tool currentness:
run selected all-tools coverage on canonical authorities using current controller; execute result-sensitive tools, record nonselection for the rest.

## 6 PD current definition

PD has two levels.

PD_core is the host-relative discrimination engine:
Generate alternatives/distinctions -> Admit/Ground -> Discriminate by result sensitivity -> Reenter.

PD_system is PD_core plus host services such as referent freeze, state/history, evidence/authority, configured execution, failure memory, closure/currentness.

System-core criterion remains:
Core_B,K(C) iff Reconstructs_B,K(C) AND Minimal_B,K(C).

A reduction of PD is invalid unless behavior attribution says where removed behavior moved in the host.

## 7 Project-priority math

Recover priority as nondominated constrained routing, not one magic scalar.

For work item w:
Eligible(w)=Material(w) AND Licensed(w) AND Reachable(w).

Priority evidence vector:
P(w)=<GoalLevel,BlockingPower,CrossProjectLeverage,InformationGain,Urgency,Cost,Risk,DependencyOrder>.

Selection:
R_K(s)=ND_preorder{w : Eligible_K(s,w)}.

Weights may be used only when the decision context licenses commensuration.
Otherwise preserve incomparable frontier.

The newest controller adds Scope/Mode and candidate-universe effects to the routing context rather than replacing this equation.

## 8 BindingTopology B=<N,E,tau>

BindingTopology is a new research-only derived relational object:
B=<N,E,tau>
N nodes/objects participating in a binding;
E binding edges;
tau typed topology/edge semantics.

It has NOT already propagated merely because it was discovered.
A prior ImproveCore pass persisted it on a research branch with tests; regression reached 196 passed.
Primitive-coordinate status remains OPEN.

Decisive test:
hold item/tool/scope/mode/goal/state/evidence/authority constant;
vary B;
test whether output/continuation changes;
then test whether B is reconstructible from existing typed relations/representation state.

Only if result-sensitive and nonreconstructible does B earn a new coordinate.
Otherwise it remains a derived view/object.

## 9 Infinite improvement guard

ImproveCore is not licensed to improve forever.

Reentry requires admitted material delta in protected result, representation/view, candidate universe, relevant scope/mode, basis, evidence/authority, or another declared result-sensitive coordinate.

Loop guard:
Reenter iff MaterialDelta AND Licensed AND BudgetRemaining AND not SeenEquivalentState.

Stop:
CLOSED_RELATIVE, PAUSED_OPEN, BLOCKED, or budget/duplicate-state stop.

New math creates ReviewRequired, never automatic kernel mutation.

## Execution order

A. repair typed work/backlog runtime.
B. add tool maturity/currentness auditor and namespace active C capabilities from CAP witnesses.
C. run C01-C49 classification and create repair/revalidation work.
D. test BindingTopology propagation against the repaired tool stack.
E. run canonical-authority replay with current tools.
F. resume Sukkos constrained production path separately.
G. create Take-6-Lab only when D/E or later work yields an admitted architectural strict gain.

This order maximizes leverage and avoids rebuilding a successor before knowing what the new foundation actually changes.
