# THE EQUATION — Runtime-Realized Current Capture 014

Date: 2026-09-25
Controller: ICC / current semantic IC-028
Status: CURRENT CAPTURE / PROVISIONAL / GENERIC RUNTIME FACADE VALIDATED
Predecessor: research/THE_EQUATION_CURRENT_CAPTURE_012.md
Runtime evidence: research/ICC_HISTORICAL_REPAIR_PATTERN_RUNTIME_REALIZATION_013.md

## Current equation

e_0
=
PEC(u,Z_0)

Z_0^+
=
P_{e_0}(Z_0)

Run_Q(u,Z_0)
=
HF001_session^TRC
[
  U_Q o C_Q o E_Q o G_Q
]
(
  Z_0^+
)

Equivalent substitution:

Run_Q(u,Z_0)
=
HF001_session^TRC
[
  U_Q o C_Q o E_Q o G_Q
]
(
  P_{PEC(u,Z_0)}(Z_0)
).

## Entry preparation

PEC freezes:
<FT,Controller,Mode_0,ModeBasis,Boundary,Authority,ClosureHandoff,Receipt>.

Bound(PEC)
precedes
SubstantiveTransition.

When Mode_0=OBSERVE_DECOUPLED:

P_e
=
TRC_obs
o
Reconcile_obs
o
DOS
o
Freeze_e.

The observer result must close before goal-directed session work consumes it.

## Core session

G_Q:
generate/instantiate live questions from the current category basis and applicable 36 projections.

E_Q:
adaptively select/order/probe/bind/execute and return evidence.

C_Q:
reconcile/admit results while preserving order interactions and OPEN/BLOCKED/CONFLICT/INCOMPARABLE.

U_Q:
update governing inquiry state, including category/frontier, evidence, coverage/currentness and continuation memory.

Every material round crosses TRC before HF-001 reentry.

## Runtime realization

Current Take-5 implementation:

PEC / P_e:
- runtime/entry_contract.py
- runtime/jane.py
- runtime/ic028_operator.py

TRC:
- runtime/tool_run_closure.py

HF recursive host:
- runtime/recursive_episode.py

Big Equation executable facade:
- runtime/inquiry_session.py

Configured run registry already contains HF001 and TRC.

## Runtime-proven behavior

Validation establishes:
- original self-application prompt auto-selects observer-first;
- observer -> reconcile -> TRC occurs before core session;
- induced TRC consequences recursively close;
- exact duplicate consequences do not create administrative echo loops;
- pending authorization remains OPEN;
- unknown consumer state cannot falsely close;
- OPEN/BLOCKED terminal closure state can be admitted when the closure contract licenses it;
- every core recursive round crosses closure before update/reentry.

Take-5 Validation run 139:
validate = SUCCESS.

## Where the 22 and 36 live

The current 22 remain a basis-relative question-category set inside Z/G_Q.

They are not the top-level equation and are not globally fixed.

The 36 remain state-dependent scope/mode projections inside question generation/applicability.

They are not 36 independent tools or categories.

## Current OPEN boundary

The following are not established by the generic facade:

- complete harvesting for every domain;
- concrete effect/verification/consumer handlers for every tool and project;
- global minimality/completeness of the current question basis;
- exact minimal continuation-equivalence relation;
- full fairness/termination theory on unbounded frontiers;
- persistent host autonomy between chat turns;
- runtime promotion of IC-028.

Therefore:

Generic TRC orchestration runtime = IMPLEMENTED_AND_TESTED.
HF recursive interleaving runtime = IMPLEMENTED_AND_TESTED.
Big Equation runtime facade = IMPLEMENTED_AND_TESTED.
All-domain binding/completeness = OPEN.
IC-028 promotion = NOT AUTHORIZED.

## Frozen current equation

Run_Q(u,Z_0)
=
HF001_session^TRC
[
  U_Q o C_Q o E_Q o G_Q
]
(
  P_{PEC(u,Z_0)}(Z_0)
).
