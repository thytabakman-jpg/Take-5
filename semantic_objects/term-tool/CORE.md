# tool

Object ID: TERM:TOOL
Type: UMBRELLA_TERM
Status: PARTIAL

## Current definition

Tool is an umbrella label. A result-sensitive use must bind the intended subtype rather than treating every executable or epistemic object as the same kind.

## Known

- question family != semantic operator
- capability != configured program
- configured program != controller
- controller != wrapper
- wrapper != runtime mechanism

## OPEN coordinates

- complete current subtype partition
- which historical aliases map to which subtype
- admission requirements by subtype

## Dependencies

- question-tool registry
- configured-run registry
- capability foundry
- tool lineage

## Protected uses

- tool lists
- tool creation
- tool comparison
- tool maturity
- tool routing

## Lifecycle rule

New evidence is additive. A later current summary may supersede an earlier summary only with explicit lineage. Unresolved coordinates are never removed merely to simplify the page.
