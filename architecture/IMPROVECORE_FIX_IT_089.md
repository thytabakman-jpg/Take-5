# ImproveCore Run 089

Date: 2026-09-26
Prompt: fix it
Status: EXECUTED / REENTRY ARCHITECTURE REPAIR

## Recovered governing problem

The current system can enumerate the five-repository corpus and preserve unresolved material, but it still cannot license the user's requested completion claim because semantic traversal, disposition, currentness propagation, and wrapper-current execution are not one closed executable episode.

## Fix

Define one parent-owned audit episode whose state is:

AuditState =
<CorpusBoundary,
 Traversal,
 Candidates,
 SemanticLedger,
 Dispositions,
 Repairs,
 Destinations,
 Currentness,
 Proof,
 OpenBlocked>.

The only terminal PASS state is:

CompleteAudit
iff
Traversal=COMPLETE
and every material candidate has a terminal semantic disposition
and every authorized repair has destination verification
and Currentness=PASS
and ProofCore=PASS.

Path enumeration, scanner completion, file movement, or zero unresolved path names are not substitutes.

## Currentness law

Any material change in an in-scope repository after CorpusBoundary freeze invalidates only the affected dependency cone and forces reentry at the earliest affected stage.

No stale branch may retain a global completion claim.

## Wrapper law

Every formal stage executes under the current configured wrapper.

For every formal tool T:

ResolveCurrentFull(T)
-> Execute
-> Admit/Normalize
-> DCC when discovery-sensitive
-> TRC/verification
-> HF1 upstream invalidation
-> HF2Applicability
-> local recurrence or typed return
-> parent reconciliation.

A stage lacking a current wrapper receipt cannot contribute to CompleteAudit.

## Semantic-disposition law

For each material artifact finding x:

Disposition(x) =
<LogicalPlacement,
 PhysicalAction,
 AuthorityEffect,
 Provenance,
 Currentness,
 ReferenceRepair,
 Verification,
 Open>.

Logical placement and physical movement are distinct.

No deletion is licensed without redundancy proof.
No private Research source is copied verbatim into public Take-5 merely to satisfy placement.

## Anti-orphan law

A candidate remains live until exactly one terminal state is recorded:

CURRENT_CONTROL
PROJECT_LOCAL
RESEARCH
EVIDENCE_HISTORY
ARCHITECTURE
EXECUTABLE
VERIFICATION
STATE_CONFIG
NAVIGATION
KEEP_HISTORICAL
DUPLICATE_WITH_WITNESS
STRICT_GAIN_ROUTED
CONFLICT
OPEN
BLOCKED.

Unlinked does not equal orphan.
Old does not equal obsolete.
New does not equal current.

## Immediate consequence

The prior full-corpus campaign remains useful evidence but is not the terminal audit episode.
Its manifests become the enumeration input to this parent-owned episode.

The next executable audit must consume current repository tips, not the stale frozen Research basis, and must produce a SemanticDispositionLedger plus ProofCore receipt before closure.

## ImproveCore disposition

FIXED:
the completion contract, reentry law, wrapper requirement, semantic disposition object, privacy boundary, and anti-orphan terminal states are now one coherent parent-owned control object.

OPEN:
the actual full semantic corpus execution remains a workload, not a solved fact.

No completion claim is emitted until that workload produces the required receipts.
