# IC-028 Foundational-Term MT / MO-MT / PD / MTA Run 046

Date: 2026-09-24
Controller lease: IC-028
Status: EXECUTED SEMANTIC MULTI-TOOL ANALYSIS
Target: prior role-separation message and user's hypothesis that IC-028 operates while Jane remains hidden/background

## IC-028 rewritten prompt

Analyze every load-bearing term in the current role decomposition rather than accepting the prose categories. Run MT across each term; run Multi-Object across Foundation, Operator, Interface and across Competence, UserFacingJane, Interface, Operator, Foundation; apply PD/result-sensitivity, MTA ownership/factorization, architecture/root-cause, representation challenge, execution truth, currentness, RTC/strict-gain and consequence closure. Decide who the user should interact with and whether Jane is interface, guardrail, tool, observer, state-maintainer, or some combination. Make one controller decision and propagate it. Avoid duplicate authority.

## 1 Term-MT

Term = <referent,type,owner,inputs,output-effect,authority,evidence,result-sensitivity,reentry>.

### System
Not one component. Composition of substrate + active controller + interfaces + external execution environment.
Hidden variable: execution environment is outside repository semantics and must not be conflated with Operator.

### Foundation
Previous definition K+S was too broad.
Factor:
Foundation = KernelLegality + CanonicalStateServices + Evidence/CurrentnessServices.
It is substrate, not actor.
It may reject/validate transitions but does not choose research goals/actions.

### Kernel
Minimal always-on legality membrane.
Identity, authority, preservation, execution truth, OPEN/conflict semantics.
Kernel must be smaller than Foundation.

### State
Durable typed research state plus lineage/currentness/provenance.
State is not Jane.

### Operator
Active policy/controller role.
Recovers job, chooses next work/capability, executes through workers, admits/integrates, verifies, reenters.
IC-028 is currently the strongest semantic specification for this role, not necessarily a standalone historical runtime.

### Interface
Boundary translation/projection role.
Input normalization + output projection + authority/user-control surface.
Interface is not necessarily Jane in full.

### Jane
Prior message overcompressed Jane to Interface.
MT finds a residual:
Jane is best typed as a hidden supervisory service/facade over Foundation, with observer/watchdog duties and human projection, but no primary episode action-selection authority.

Candidate Jane residual:
Jane = Interface + FoundationWatch + ContinuityWatch + StateProjection.
This is distinct from Operator.

### Observer
Goal-decoupled evidence producer.
Observer can be a Jane service/tool available to Operator.
Observation does not itself authorize action.

### Guardrail
A constraint/check, not an agent.
Guardrails belong to Kernel/Foundation. Jane can expose/enforce them but should not be their semantic owner merely by name.

### Tool
Callable capability selected by Operator.
Jane-as-a-whole should not be a normal tool because Jane carries cross-episode supervisory/interface state.
However Jane services (Observe, CurrentnessCheck, HumanView, ContinuityWatch) can be tools/services used by Operator.

### Background
Ambiguous.
Distinguish:
background semantic service = always logically applicable;
background process = actually scheduled/running independently.
Take-5 currently supports the former, not the latter in chat.

### Hidden
Means not normally surfaced to user, not unaccountable.
Hidden services still require receipts when result-sensitive.

### User
External authority/source of goals, permissions and corrections; not a runtime component.

### Competence
Previous additive equation was too naive.
Competence is bottleneck/interaction sensitive, not a scalar sum.

Better:
Competence_user(S,J) = Ability of composed system S to achieve job J under protected constraints and continuation horizon.

Necessary coordinates:
goal recovery, model quality, capability coverage, selection quality, execution realizability, evidence/admission, propagation, persistence, reentry, interface fidelity.

A severe zero in one load-bearing coordinate can dominate the composition.

### UserFacingJane
A presentation identity, not an architectural primitive.
It may denote the whole composed experience but must not imply Jane owns all internal functions.

### Keep Jane up to date
Needs exact semantics:
For every admitted material delta d, update the Jane-visible supervisory projection and affected watch state before episode closure.
This is a consequence obligation, not a vague instruction.

## 2 Multi-Object: Foundation x Operator x Interface

Foundation x Operator:
Foundation constrains and services Operator. Operator may propose state transitions; Foundation validates/persists. Foundation must not choose policy; Operator must not bypass Foundation.

Operator x Interface:
Interface transmits intent/authority and renders result. Operator owns work policy. Interface must not silently rewrite goals beyond typed normalization.

Foundation x Interface:
Interface reads projections of canonical state and submits authorized user changes. It must not become a second state store.

Three-way residual:
A missing SUPERVISORY PROJECTION/WATCH role remains after F/O/I factorization.
Without it, cross-episode continuity, drift detection, currentness surfacing and hidden observation have no natural owner.

This residual is the legitimate Jane role.

Therefore F/O/I was not complete.

## 3 Multi-Object: Competence x Jane x User x Interface x Operator x Foundation

Key residual:
User-facing competence is emergent from composition, but control authority is not emergent; it must remain explicitly located.

Best topology:
User <-> Interface(Jane)
Jane supervisory services <-> Foundation
Interface -> Operator episode lease
Operator -> Foundation services/workers
Operator -> Jane services when observation/currentness/continuity projection is useful
Foundation -> Jane update obligation on admitted material delta
Operator -> User only through Interface projection by default.

Jane is therefore both the interface identity and supervisory sidecar, but not primary operator.

## 4 PD / result sensitivity

Discriminator A: Remove Jane supervisory watch while retaining Interface+Foundation+Operator.
Predicted changed results:
cross-episode drift/currentness defects become less visible; human projection can stale; migration omissions are less likely to be surfaced automatically.
Therefore Jane supervisory residual is result-sensitive.

Discriminator B: Give Jane primary action-selection authority in addition to IC-028 Operator.
Predicted changed results:
duplicate authority, conflicting plans, longer path, ambiguity over completion owner.
No protected gain established.
Reject duplicate operator role.

Discriminator C: Remove Interface identity and let user speak directly to Operator.
Core research can still run, but state/authority projection and continuity experience degrade.
Interface is independently result-sensitive.

Discriminator D: Move kernel legality into Jane.
Results become name/lifecycle coupled; historical or alternate operators can bypass/duplicate it.
Reject. Kernel remains below Jane.

## 5 MTA factorization

Reconstructed architecture:

K = Kernel
S = CanonicalStateServices
F = <K,S,Evidence,Currentness,Persistence>
O = IC028ClassOperator
J = <Interface,ObserverServices,ContinuityWatch,SupervisoryProjection>
X = ExternalExecutionEnvironment

System = Compose(F,O,J,X)

Authority:
User grants/changes authority.
F enforces legality.
O owns episode policy under ControllerLease.
J observes/projects/surfaces and may trigger candidate obligations, but cannot unilaterally execute them unless O admits/selects them or a separately authorized maintenance lease exists.

## 6 MT modes

EXPAND:
Jane can contain observer/currentness/continuity services beyond UI.

CONTRACT:
Jane's irreducible role is supervisory continuity + interface, not research policy.

INWARD:
Kernel remains below Jane; operator intelligence remains outside Jane.

OUTWARD:
IC-028 should consume Jane observations and must emit admitted deltas back into Jane supervisory projection.

ISOLATE:
IC-028 without Jane can solve an episode but loses cross-episode/watch advantages.
Jane without IC-028 can observe/guard/project but cannot competently drive research.

COUPLE:
The pair is stronger when the coupling is asymmetric and typed:
Jane -> evidence/alerts/context -> IC-028
IC-028 -> admitted deltas/receipts -> Jane
Foundation mediates legality/state.

## 7 Architecture/root cause

Prior binary choice "Jane or IC-028" was false.
The missing role was supervisory sidecar.

Root cause of confusion:
the label Jane had accumulated interface, operator, delegation and system identity responsibilities without a typed ownership test.

## 8 Execution-truth/currentness challenge

Current jane.py only partially realizes the new Jane role.
It has Interface/HumanView and wrappers, but not a robust ContinuityWatch or automatic supervisory update hook.

Therefore:
Jane_supervisory_semantics = ADMITTED CANDIDATE.
Jane_supervisory_runtime = PARTIAL/OPEN.

IC-028 operator semantics = strongest current operator candidate.
Fully configured runtime over continuation engine = PARTIAL/OPEN.

## 9 RTC / strict-gain comparison

Candidate A: Jane becomes operator.
No strict gain; duplicates O.

Candidate B: Jane only interface.
Loses result-sensitive supervisory residual.

Candidate C: Jane = interface + supervisory sidecar; IC-028-class Operator owns episodes; Foundation owns legality/state.
Strict gain candidate over A/B because it preserves supervision without duplicate action-selection authority.

SELECT C.

## 10 Consequence closure

Required consequences:
1 Jane semantics updated from interface-only to interface+supervisory sidecar.
2 ControllerLease remains exclusive for primary Operator.
3 Foundation admitted material delta must emit Jane supervisory update.
4 IC-028 Operator may query Jane observer/currentness/continuity services.
5 Jane alerts become Work candidates, not self-executing mutations.
6 HumanView remains Jane projection.
7 Kernel/Foundation ownership remains independent of Jane.
8 CompletionCert must include supervisory projection currentness when job depends on cross-episode continuity.

## 11 Decision

The user should normally talk to IC-028 as the research operator when explicitly doing research/system work, while Jane remains mostly hidden as interface/supervisory continuity layer.

At product-conversation level the visible name can still be Jane, but architectural control must not pretend Jane is the operator.

For this current experiment:
IC-028 retains controller lease.
Jane is admitted helper/supervisory service.
IC-028 must keep Jane current after admitted material deltas.
Jane may surface material drift/continuity alerts to IC-028 as candidate Work.
Jane cannot override IC-028's episode policy.

This is the selected architecture pending runtime fixture validation.
