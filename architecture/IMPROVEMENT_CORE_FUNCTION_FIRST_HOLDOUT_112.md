# ImprovementCore Function-First Heterogeneous Holdout 112

Date: 2026-09-26
Status: VALIDATED CURRENT EXPERIMENT / NOT SYSTEM-WIDE PROMOTED
Controller: ImprovementCore
Canonical repository: thytabakman-jpg/Take-5

## Recovered historical trigger

The September 25 PD discovery established the exact mathematical equivalence between
existence-plus-uniqueness and total single-valuedness.

For a relation R subset X x Y:

Function(R)
iff
forall x in X exists! y in Y such that R(x,y).

The fixed-frame PD/ED core already had:

ED(F) iff EX(F) and UN(F).

Therefore the bare determination core is function-shaped when domain and codomain are typed.

This did not collapse the wider PD project into one function. Determination remained distinct
from fidelity, supplementation, representation, audit, alternative generation, and compression.

PD's function-first goal-change proposal remains pending explicit approval. This experiment does
not alter PD:G0.

## Later function-first refinement

The first outer-function proposal

T^sharp_K : X_T -> P(Y_T) x D_T x W_T

was too weak because it can lose which disposition and witness belongs to which branch.

The repaired native carrier is branch-coupled:

B_T
=
Y_T x Sigma_T x D^b_T x W^b_T.

R_T
=
P(B_T) x D^i_T x W^i_T x Receipt_T.

The whole named configured invocation is therefore represented as:

T^cfg_{J,K}
:
I_T
->
R_T,

I_T
=
X_T x Sigma_T x Context_T.

This keeps underlying semantics relational:

R_{T,K}
:
X_T
rightrightarrows
Y_T.

The function surface is a configured interface, not a claim that every inner semantic relation
is deterministic.

## ImprovementCore question

Can the current Take-5 tool ecosystem be losslessly re-expressed through one common configured
function IR without hidden per-tool exceptions?

The earlier ImprovementCore function-first run specified the heterogeneous holdout:

PD or PDAudit,
MT,
Architecture,
ICC-123,
ICC-128,
ImprovementCore,
Tool Conductor.

This experiment executes that request.

## Executable experiment

Runtime:

runtime/configured_function_ir.py

Regression:

tests/test_configured_function_ir.py

The encoder reads current Take-5 configured-run identity, manifest identity, and compilation
witnesses. It does not infer missing semantics from tool names.

Every registered object receives the same structural form:

R_{T,K}: X_T rightrightarrows Y_T

T^cfg_{J,K}: I_T -> R_T

I_T = X_T x Sigma_T x Context_T

B_T = Y_T x Sigma_T x D^b_T x W^b_T

R_T = P(B_T) x D^i_T x W^i_T x Receipt_T.

Configured semantic identity and effective runtime realization remain separate statuses.

## Holdout result

MT:
configured identity CLOSED_RELATIVE;
runtime ENVIRONMENT_BOUND.

PD:
configured identity OPEN because only generic manifest identity is present;
native runtime UNRECOVERED.

PDAudit:
same residual class as PD.

Architecture:
same residual class as PD.

ImprovementCore:
configured identity OPEN because its manifest remains generic-only;
runtime ENVIRONMENT_BOUND.

Tool Conductor:
configured identity OPEN because its manifest remains generic-only;
runtime SELF_CONTAINED.

ICC123:
BLOCKED_UNREGISTERED_NAMED_OBJECT.

ICC128:
BLOCKED_UNREGISTERED_NAMED_OBJECT.

No ICC neighboring object is substituted.

## Material discovery

The function-first architecture does not fail because functions are the wrong mathematics.

It fails at a deeper current-system seam:

named formal-object identity
!=
configured-tool registry identity
!=
explicit protected-behavior manifest identity
!=
native realization identity.

The IR can represent these layers uniformly, but Take-5 does not yet supply all four layers for
the heterogeneous holdout.

This explains why function-first discoveries can be repeatedly rediscovered without becoming a
stable system ontology.

## Improvement

The new executable audit makes the distinction machine-visible.

Function notation can no longer promote PD, Architecture, ImprovementCore, Tool Conductor, ICC123,
or ICC128 to a false complete state.

A tool/function surface is green only relative to recovered identity evidence; runtime realization
remains separate.

## Next frontier

The next high-leverage work is no longer to invent a richer function equation.

It is to close the identity graph:

1. explicit tool-specific protected-behavior manifests for ImprovementCore and Tool Conductor;
2. explicit native runtime or typed external realization for PD/PDAudit/Architecture;
3. canonical registered identity for ICC123 and current ICC128, or an explicit disposition that
   they are not members of the configured-tool registry;
4. preservation witnesses connecting current FullMath/RunSpec objects to the configured-function IR.

Only after those holdout residuals close does promotion of the functional IR become warranted.

## Disposition

MATERIAL IMPROVEMENT.

The function-first breakthrough is now represented in Take-5 as executable evidence rather than
remaining only in Reaserch/conversation history.

SYSTEM-WIDE FUNCTION IR PROMOTION:
OPEN.

PD CANONICAL GOAL CHANGE:
NOT PERFORMED.


## Validation receipt

Take-5 Validation:
36265194313 SUCCESS.

Capability Preservation:
36265194305 SUCCESS.

Initial experiment merge:
9bfc10803c048d53f5cb21c27c494a972d4431fa.
