# ImproveCore Math Corpus Integration 090

Date: 2026-09-26
Status: CROSS-LINEAGE RECOVERY / STRICT-GAIN CANDIDATE
Controller: IC-028 / ImproveCore
Scope: Take-2, Take-3, Take-4, Take-5, Reaserch, and recovered prior-chat mathematics

## Governing question

What mathematics already discovered across the system lineage changes ImproveCore's
controller semantics, state, routing, recurrence, closure, persistence, or recovery?

The goal is not to maximize equation count.
The goal is to recover every material mathematical result, preserve its provenance,
and import only result-sensitive strict gains into ImproveCore.

## Recovery basis

Authoritative navigation used:
- Reaserch Core System Mathematics Canonical Index 2026-09-25
- Reaserch Mathematical Result Map
- Reaserch Mathematical Corpus Index
- Reaserch Map of Math Maps IC-028 Pass 002
- Reaserch every-map unification crosswalk
- Take-2 operator/modifier/router/update/state experiments
- Take-4 controller mathematics and cumulative-autonomy experiments
- Take-5 current ImproveCore, HF1, HF2, question-search, mode-cube, wrapper,
  RootCause, execution, closure, concurrency and recovery mathematics
- recovered prior-chat exact equations whose semantics are already represented
  in current repository objects

This integration does not restore superseded authority merely because an older
formula is more detailed.

## Result 1 — common typed-map substrate

Recovered canonical object:

F : X ⇉ Y
with F subseteq X x Y.

Total deterministic functions are the special case with exactly one y for every x.

Benefit to ImproveCore:
- action generation, routing, state transition and admission need not pretend to be total functions;
- plural actions, OPEN, BLOCKED and nondeterministic returns remain mathematical values;
- controller interfaces can use correspondences instead of forcing arbitrary unique winners.

Disposition:
INTEGRATE AS TYPE DISCIPLINE.

## Result 2 — continuation-equivalence state

For active job J and basis K:

h1 ≡_(J,K) h2
iff every protected licensed continuation observes them equivalently.

Canonical fixed-basis state:

Q*_(J,K) = Hist_K / ≡_(J,K).

Representation sufficiency:

ker(q) subseteq ≡_(J,K).

Exact coarsest minimality:

ker(q) = ≡_(J,K).

Benefit to ImproveCore:
- the controller's semantic state Z is not justified merely because fields exist;
- state compression is safe only when continuation-relevant distinctions are preserved;
- memory, mode, authority, evidence or discovery coordinates belong in state exactly when
  forgetting them changes legal future continuation.

Disposition:
INTEGRATE AS STATE ADEQUACY TEST.
Global computable quotient remains OPEN.

Runtime witness:
runtime/improvement_core_math_spine.py::representation_sufficient

## Result 3 — ambient system algebra

Current ambient factorization:

T = <K,S,O,G,M,C,R,U>.

Benefit to ImproveCore:
- controller logic, state, operator work, generator work, modifiers, judgment,
  routing and mutation are not interchangeable;
- adding a semantic distinction does not automatically justify adding another durable tool/file;
- ImproveCore can remain one controller while using typed subroles.

Disposition:
PRESERVE AS AMBIENT FACTORIZATION.
Do not expand controller surface merely to mirror role count.

## Result 4 — four-core cognitive basis

Current relative operator basis:

O_core = {
  DIFFERENTIATE,
  RELATE,
  RECONSTRUCT,
  STRENGTHEN
}.

Historical FACTOR, CLASSIFY/TYPE and DIAGNOSE survive as macros/compositions under current basis.

Benefit to ImproveCore:
- broad search can be generated from four reusable cognitive transforms instead of blindly
  invoking every named tool;
- full configured tools retain identity where protected behavior is not reconstructible.

Disposition:
USE AS CANDIDATE-GENERATION PRIMITIVES, NOT TOOL-IDENTITY ERASURE.

## Result 5 — router mathematics is set-valued

Take-2 and Take-4 independently converged on:
eligibility first;
then partial preference/dominance;
then nondominated frontier;
then optional selection.

No canonical scalar utility is justified.

Benefit to ImproveCore:
- protected behavior, authority and reachability are hard admissibility constraints;
- incomparable options remain plural;
- cost/risk can shape a Pareto frontier without overriding protected behavior;
- a single winner requires an additional licensed selection rule.

Disposition:
INTEGRATE NOW.

Runtime witness:
runtime/improvement_core_math_spine.py::nondominated_frontier

## Result 6 — protected capability preservation

Take-4 controller math requires candidate successor S' to preserve protected capability B:

S preceq_B S'.

Strict system improvement requires witnessed non-regressive gain, not a newer artifact.

Benefit to ImproveCore:
- no local route can be admitted merely because goal gain is high;
- preservation moves from soft score to admissibility gate;
- successor selection and architecture changes inherit anti-loss mathematically.

Disposition:
INTEGRATED INTO NONDOMINATED ROUTING ADMISSIBILITY.

## Result 7 — cumulative autonomous preservation

Take-4 reconstructed the governing longitudinal target:
verified reusable contributions from earlier episodes remain reachable unless later evidence
explicitly revises or retracts them.

Benefit to ImproveCore:
- anti-loss applies across episodes, not just within one transition;
- recovering an old capability later does not excuse silent forgetting now;
- contribution reachability becomes a regression coordinate.

Disposition:
INTEGRATE NOW AS LONGITUDINAL INVARIANT.

Runtime witness:
runtime/improvement_core_math_spine.py::cumulative_preservation

## Result 8 — Update U is a typed transition law

Take-2 recovered:

U_K : Sigma_K x D_K ⇀ P_nd(Sigma_K)
      + {NOOP,OPEN,BLOCKED,FAIL,CONFLICT}.

Primitive update families:
ACCUMULATE;
REVISE/REPLACE;
STRUCTURAL/TOPOLOGY CHANGE;
RETRACT/INVALIDATE.

Benefit to ImproveCore:
- mutation result is not synonymous with success;
- OPEN/BLOCKED/CONFLICT remain first-class transition outcomes;
- rollback/merge/atomicity are controls or compositions, not new primitive update families.

Disposition:
PRESERVE IN STATE-COMMIT / TRC / ADMISSION CONTRACTS.

## Result 9 — state is behavioral, not a file schema

Take-2 state result:
minimal persistent semantic carrier sufficient for future behavior.

Current coordinate families:
identity/topology;
active semantic content;
epistemic/evidence;
authority/license;
history/temporal trace.

Benefit to ImproveCore:
- recovery anchors, ledgers and YAMLs are views/encodings, not the state itself;
- any new state field must justify itself through future-continuation discrimination.

Disposition:
USE CONTINUATION SUFFICIENCY AS THE TEST.

## Result 10 — mode geometry

Recovered geometry:

Mode = Breadth x Direction x Coupling

Breadth in {EXPAND, CONTRACT}
Direction in {OBSERVE, ACT}
Coupling in {DECOUPLED, COUPLED}.

Broad observer:
EXPAND + OBSERVE + DECOUPLED.

Focused resolution:
CONTRACT + OBSERVE + DECOUPLED.

Whole-system ImproveCore:
EXPAND + ACT + COUPLED.

Benefit:
mode selection becomes a controller coordinate rather than hidden prompt style.
Observer and focused are not opposites.

Disposition:
ALREADY INTEGRATED IN candidate regime 085.

## Result 11 — scope-transition x mode geometry

Recovered architecture:
six scopes;
36 directed source->target scope edges;
six mode faces;
partial 6x6x6 face-indexed control surface.

A_F subseteq Scope_src x Scope_dst x ModeFace.

Benefit to ImproveCore:
- 36/216 is coverage/search geometry, not an ontology and not mandatory brute-force execution;
- tool family answers WHAT;
- scope edge answers WHERE FROM/TO;
- mode answers HOW;
- contract answers UNDER WHAT protected job;
- applicability is partial and witness-gated.

Disposition:
USE FOR CANDIDATE APPLICABILITY AND COVERAGE, NOT BLIND EXPANSION.

## Result 12 — question-search fixed point

Recovered question object:
q=<Issue,Target,AnswerSpace,Presuppositions,ResolutionCondition,ProtectedResult>.

Recovered universe search:

Q* = Fix(
  Quotient(
    Normalize36(
      Atom(
        ExpandLineage(H)
      )
    )
  )
)

with composite residual preservation.

Benefit to ImproveCore:
- named tools do not define the question universe;
- question families can merge, split, expand or remain composite based on protected result;
- 22 current families are basis-relative search categories, not universal ontology.

Disposition:
USE INSIDE GENERATION/INQUIRY, NOT AS TOP-LEVEL CONTROLLER DIMENSION.

## Result 13 — Big Equation / wrapper orchestration

Recovered current equation:

Run_Q(u,Z0)
=
HF001_session^TRC[
  U_Q o C_Q o E_Q o G_Q
](
  P_{PEC(u,Z0)}(Z0)
).

Meaning:
entry contract / preparation;
question generation;
evidence execution;
reconciliation/admission;
state update;
TRC;
HF1 recurrence.

Benefit to ImproveCore:
- wrapper semantics must be part of configured execution, not assistant convention;
- observation-first preprocessing must close before goal-directed consumption when selected;
- question generation and execution remain distinct.

Disposition:
PRESERVE AS EXECUTION ENVELOPE.
ImproveCore-specific policy remains the C+ controller inside that envelope.

## Result 14 — HF1 and DCC

Recovered prior/current relation:
discovery closure determines whether representation/basis distinctions are still safe;
HF1 classifies upstream invalidation and earliest reentry.

Benefit:
- reentry must trigger not only on result change but on search, discovery, representation,
  candidate-universe, authority, evidence, scope, mode, or runtime-realization change when material.

Disposition:
INTEGRATE REOPEN LAW NOW.

Runtime witness:
runtime/improvement_core_math_spine.py::reopen_required

## Result 15 — HF2 local recurrence versus global recurrence

Recovered two-scale recurrence:

local:
same capability C is reapplied to its changed normalized successor while material local
delta and live local frontier remain under stable upstream basis.

global:
parent ImproveCore regenerates/reselects across capabilities/jobs after local return.

Benefit:
- local recursive refinement is not the same thing as global controller continuation;
- a child capability cannot self-promote into parent authority;
- local RELATIVE_CLOSE never implies ImproveCore COMPLETE.

Disposition:
ALREADY ACTIVE THROUGH HF002 substrate and recursive parent/child boundary.
Protect explicitly.

## Result 16 — augmented ImproveCore controller

Current controller:

C+_(J,K)
=
<Z x M,
 D,
 A+_(J,K),
 pi+_(J,K),
 T+_(J,K),
 Tau+_(J,K)>.

Learning memory blocks unchanged NO_GAIN/REJECTED/FAILED routes unless a changed coordinate
intersects the recorded dependency footprint.

Benefit:
negative evidence changes future admissibility.
Recursive child runs are typed actions, not independent authorities.

Disposition:
CURRENT.

Strict gain from this integration:
pi+ must be interpreted as a set-valued admissible-frontier policy before any optional chooser.

## Result 17 — discovery closure / coevolving basis

The canonical Reaserch theorem extends fixed-basis closure to endogenous discovery over
basis-scope states, separating cumulative discovery D_seen from minimal active basis MinBasis(D_seen).

Benefit:
- ImproveCore's candidate universe can change because observation discovers new relations;
- old closure is invalid when the discovery basis expands materially;
- retaining all discovered facts and retaining every active route are different problems;
- redundant discovery routes can be minimized without forgetting the discovery ledger.

Disposition:
IMPORT AS CONTROLLER LAW.
Scalable general implementation remains OPEN.

## Result 18 — closure is basis-relative, not resource-relative

Recovered closure requirements across RTC, HF, TRC and controller termination:
- terminal consequence closure;
- current upstream basis;
- no live material frontier;
- OPEN/BLOCKED/CONFLICT explicit;
- resource exhaustion cannot masquerade as completion.

Benefit:
ImproveCore should not equate max_rounds/max_iterations with closure.

Disposition:
INTEGRATE NOW.

Runtime witness:
runtime/improvement_core_math_spine.py::closure_disposition

## Result 19 — RootCause mathematics

RootCause seeks the smallest stable upstream generator of a recurring failure class, using
a nondominated rootness frontier and HF2 smaller-generator challenge.

Benefit:
- repeated ImproveCore failures can be attacked as generator recurrence rather than repaired symptom by symptom;
- root-cause output remains a child/local result until parent admission.

Disposition:
ALREADY PROMOTED LOCALLY.
Use as selected capability when recurrence evidence exists.

## Result 20 — execution truth ladder

Recovered distinction:
CONSIDERED -> SELECTED -> BOUND -> DELEGATED -> EXECUTED -> RECEIPTED -> REINTEGRATED -> VERIFIED.

Benefit:
- named availability or semantic simulation does not count as tool use;
- ImproveCore can audit where the execution chain actually failed;
- capability completeness does not imply program completeness.

Disposition:
PRESERVE AS EXECUTION EVIDENCE INVARIANT.

## Result 21 — tool speed / factorization

Repeated math:
36 questions/cells do not imply 36 full independent runs.

Shared kernel/setup, factor reuse, sparse residual work and affected-cone revalidation reduce cost.

Benefit:
- optimize only after adequacy;
- cache/reuse shared basis;
- rerun affected descendants rather than whole system;
- route cheap nondominated packages instead of broad attack when evidence permits.

Disposition:
USE AS PERFORMANCE POLICY AFTER CORRECTNESS.

## Result 22 — artifact completeness / canonical reconstruction

Current(o) implies a canonical recoverable reconstruction surface for load-bearing objects.
Index presence is not reconstruction.

Benefit:
- every ImproveCore mathematical gain must be recoverable from current anchors/runtime/tests;
- distributed provenance alone cannot count as active capability.

Disposition:
EXTEND RECOVERY ANCHOR FOR THIS MATH SPINE.

## Result 23 — concurrency mathematics

Concurrent ImproveCore workstreams conflict when write/read-write footprints overlap.
Fail closed when registry/base snapshot/conflict status is incomplete.

Benefit:
- mathematical improvement work cannot silently overwrite concurrent strict gains.

Disposition:
PRESERVE PRE-MUTATION PREFLIGHT.
This integration was isolated on a stacked branch.

## Result 24 — outside learning mathematics

Earlier external/operator research introduced Bayesian updating, predictive-processing/free-energy,
MDL, rate-distortion, OODA/feedback and actor-critic style learning analogies.

Benefit to ImproveCore:
these are useful comparators and candidate inspiration for information/cost tradeoffs.

Disposition:
DO NOT IMPORT AS GOVERNING OBJECTIVE.
No evidence licenses replacing protected, partially ordered controller semantics with one
probabilistic reward or free-energy scalar.

## New ImproveCore controller interpretation

The current C+ mathematics is strengthened as:

1. Z must be continuation-sufficient relative to active J,K.
2. A+ is generated under typed relation/mode/scope geometry.
3. Hard legality/preservation/authority/reachability filters apply first.
4. pi+ returns a nondominated frontier, not necessarily one action.
5. optional selection over that frontier requires an explicit policy/witness.
6. T+ returns typed transition dispositions.
7. admitted deltas update evidence, discovery, learning and cumulative contribution state.
8. material discovery/representation/authority/runtime deltas reopen the earliest affected layer.
9. HF2 closes same-capability local frontiers; parent ImproveCore owns cross-capability recurrence.
10. Tau+ returns RELATIVE_CLOSE only with terminal TRC, current basis, no live material frontier,
    and preserved reachable verified contributions.
11. resource bounds return RESOURCE_STOP/OPEN, never COMPLETE.
12. state/basis compression requires continuation-preservation witnesses.

## Immediate strict gains implemented

runtime/improvement_core_math_spine.py

implements finite witnesses for:
- hard-admissibility Pareto routing;
- nondominated frontier preservation;
- continuation-relative representation sufficiency;
- basis-relative closure disposition;
- material reopen triggers;
- cumulative contribution preservation/reachability.

tests/test_improvement_core_math_spine.py

guards these recovered invariants.

## What remains OPEN

- computable general evolving continuation quotient;
- exact policy for choosing one action from a plural nondominated frontier;
- quantitative information-loss measure;
- complete problem-generation relation P(z);
- scalable coevolving discovery closure and MinBasis;
- path coherence across basis transitions;
- global primitive/minimality results;
- global termination/fairness on infinite frontiers;
- universal host enforcement;
- all-domain capability reachability;
- automatic promotion of every recovered mathematical theorem into runtime where it is useful.

## Anti-loss conclusion

The mathematics corpus does not point toward a single bigger equation with every symbol embedded.

It points toward one controller governed by a small set of stable laws:
typed partial/set-valued maps;
continuation-relative state;
hard admissibility;
nondominated selection;
mode/scope-aware generation;
typed transition truth;
discovery-sensitive reentry;
local/global recurrence separation;
basis-relative closure;
cumulative verified preservation.

Everything else remains a specialization, witness, configured tool, search geometry or historical derivation.
