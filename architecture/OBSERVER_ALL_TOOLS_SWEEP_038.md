# Observer All-Tools Sweep 038

Date: 2026-09-25
Mode: OBSERVE_DECOUPLED
Controller: IC-028
Frozen target: IC + Jane + math-first wrapper plan
Protected constraints:
- do not rebuild IC;
- do not rebuild Jane;
- wrapper may be rebuilt greenfield;
- preserve authority, provenance, OPEN/BLOCKED, controller lease, and current behavior.

## Rewritten governing prompt

IC, take the lead in observer mode.

Freeze the current IC/Jane/wrapper plan before changing anything. Run the complete capability registry and the newer mathematical lenses against the frozen plan. Use every capability only for its actual typed job; do not invoke tools ceremonially. Collapse duplicate findings to their generating roots.

Preserve IC and Jane as protected suboperators unless a narrowly scoped interface patch is required and regression-tested. Rebuild the wrapper from the mathematics up if that is the minimal architecture.

Resolve the mathematics first, then derive the goal, architecture, tool package, closure boundary, update rule, Jane synchronization, and reentry condition. Implement the smallest complete candidate in parallel with the current wrapper. Run regression, hostile-review, holdout, and ablation evidence. Preserve OPEN where evidence is incomplete. Promote nothing merely because it is newer.

Finish with one coherent equation, executable code, tests, and a precise statement of what remains OPEN.

## C01-C49 sweep

| Tool | Result on frozen plan |
|---|---|
| C01 Task/Object Typing | ACCEPT. Exact task is wrapper completion; IC and Jane are protected referents. |
| C02 Referent Continuity | ACCEPT. IC-028 and Jane remain the same controller/supervisor objects, not replacements. |
| C03 History Supersession | ACCEPT. Plan 037 and Decision 036 are current design evidence; old wrappers remain rollback evidence. |
| C04 Source Result Freeze | REQUIRED. Current IC/Jane behavior must be frozen by regression witnesses. |
| C05 Target Job Freeze | ACCEPT. Finish the wrapper; no IC/Jane rebuild. |
| C06 External Dependence | ACCEPT. GitHub Actions is the runtime validation dependency; no hidden domain dependency is required. |
| C07 Hidden Dependency | FINDING. Wrapper ordering, IC selector, Tool Run Closure, admitted update, and Jane sync are coupled. |
| C08 Dependency Spine | ACCEPT. Entry -> Observe -> Formalize -> FreezeMath -> Goal -> Architect -> IC -> Closure -> Update -> JaneSync -> Reentry. |
| C09 Layer Control Mapping | ACCEPT. Jane=interface/supervision; wrapper=orchestration; IC=selection/control; runtime=execution; closure=verification/admission boundary. |
| C10 Representation Attack | FINDING. Old big-equation stage representation hides math freeze and conflates closure/reentry. |
| C11 Result Sensitivity | FINDING. Three live roots: selector math, closure/state boundary, wrapper ordering. |
| C12 Invariant Localization | ACCEPT. Protected invariants are lease/identity, Jane non-selection, authority/provenance, closure-before-admission, OPEN/BLOCKED preservation. |
| C13 Attribution Separation | ACCEPT after separating Tool Run Closure from the HF delta router. |
| C14 Hostile Review | FINDING. Attack surfaces: frozen-math mutation, job/target drift, authority escalation, incomplete cover, false closure. Candidate code guards each. |
| C15 Novelty Comparator | NONBLOCKING. The gain is integration/typing, not a claim that the component mathematics is novel. |
| C16 Change Frontier | ACCEPT. Minimal frontier is new wrapper + optional IC selector + Jane relevance predicate + naming separation. |
| C17 Failure Diagnosis | ACCEPT. Failures map to the same three root classes. |
| C18 Root Cause Escalation | ACCEPT. Root is architecture accumulated before formal object boundaries were frozen. |
| C19 Recursive Discovery | ACCEPT. No fourth independent root discovered. |
| C20 Rival Reconstruction | ACCEPT. Rival A: patch old inquiry_session. Rival B: parallel greenfield wrapper. B dominates on isolation/rollback risk. |
| C21 Successor Generation | ACCEPT. Greenfield wrapper candidate generated. |
| C22 Improvement Frontier | ACCEPT. Remaining frontier is validation and later typed tool-contract admission. |
| C23 Source/Target Relation | ACCEPT. Design 037 is source; runtime wrapper branch is target. |
| C24 Defect Repair | EXECUTED on branch. |
| C25 Behavior Compression | ACCEPT. Preserve IC/Jane; compress duplicate wrapper control semantics. |
| C26 System Architecture Improvement | ACCEPT. Wrapper rebuilt as typed orchestration around protected suboperators. |
| C27 Subsystem Improvement | ACCEPT. Selector extension is optional and localized. |
| C28 Component Improvement | ACCEPT. Jane relevance predicate and selector are independent components. |
| C29 Interface Improvement | ACCEPT. Frozen-math, external-job, closure, and sync contracts are explicit. |
| C30 Boundary Redesign | ACCEPT. Tool Run Closure is the execution-to-admission boundary. |
| C31 Cross-Layer Improvement | ACCEPT. Jane sync occurs after admitted state delta, not as a competing action controller. |
| C32 Leverage Routing | ACCEPT. Highest leverage is wrapper first, then selector adapter, then tool-contract expansion. |
| C33 Strict Gain | VALIDATION-DEPENDENT. Requires branch regression + holdout evidence before canonical claim. |
| C34 Regression Check | RUNNING/PASSING on GitHub Actions for early commits. |
| C35 Incomparable Successors | ACCEPT. Old and new wrappers remain side-by-side until validation closes. |
| C36 Recompute Architecture | ACCEPT. New full equation below. |
| C37 Transfer Provenance | ACCEPT. Changes isolated on branch with commit lineage. |
| C38 Transfer License | ACCEPT. User authorized solution; production promotion still remains separate. |
| C39 Target Effect | VALIDATION-DEPENDENT. Tests must show actual math-first order and freeze enforcement. |
| C40 Existing Coverage | ACCEPT. C01-C49 registry plus learning toolkit considered. |
| C41 Downstream Rescue | ACCEPT. New learning tools can enter later through typed IC contracts rather than wrapper hard-coding. |
| C42 Transfer Disposition | ACCEPT. D4/D6/D8 and learning lenses remain selectable packages. |
| C43 Consequence Handoff | ACCEPT. IC result -> Tool Run Closure -> admitted update -> Jane synchronization. |
| C44 Regression Verification | RUNNING/PASSING for current branch wave. |
| C45 Holdout | ADDED. Unfamiliar recursive holdout must reach a stable answer by reobservation/reentry. |
| C46 Ablation | EVIDENCE ADDED. Historical max-hit selector and exact least-cost selector are directly contrasted in tests. |
| C47 Relative Closure | OPEN until latest validation wave passes. |
| C48 Raise Ceiling | DEFERRED. Admit more tools only after wrapper closure; no new architecture required now. |
| C49 Corpus Navigation | ACCEPT. Current Take-5 runtime, tests, architecture, and controller files were navigated directly. |

## New-tool sweep

The learning toolkit does not reveal a new mandatory wrapper stage.

- D6/D8: selectable IC lenses; not wrapper stages.
- Kolb: experience-learning loop; non-applicable to wrapper control.
- DIKW: useful analogy for state transformation; no new control invariant.
- Predictive Processing: supports error-driven reentry; already represented by verification/reentry.
- Bayesian update: useful when IC's mathematical object is probabilistic; not global.
- Active Inference: policy selection lens; not global.
- Actor-Critic: adaptive policy-learning lens; not global.
- Rate-Distortion: directly supports compression/minimal representation but remains an IC-selectable lens.
- OODA: overlaps wrapper feedback structure but adds no missing invariant.
- Functional Stack: confirms layer separation among Jane/wrapper/IC/runtime/verification.

## Root quotient

All findings quotient to three root obligations:

1. Selection mathematics
   - replace optional execution selection with exact least-cost sufficient coverage when the math-first wrapper requests it;
   - preserve historical selector as default/rollback.

2. Closure and state boundary
   - Tool Run Closure is C_TR;
   - HF material-delta routing is R_delta;
   - only admitted closure state reaches persistent update;
   - Jane sees admitted supervisory deltas.

3. Wrapper ordering
   - observer first;
   - formalize and freeze mathematics before operational goal and architecture;
   - IC acts only after typed compilation;
   - fixed-point closure is relative to the question result plus no live reentry trigger.

## Final wrapper mathematics

Let beta be Jane's bound entry contract.

    beta = B_J(u)

For round n:

    o_n    = O_beta(z_n)
    m_n    = Phi(o_n, beta)
    mbar_n = Freeze(m_n)
    g_n    = G(mbar_n, beta)
    a_n    = A(mbar_n, g_n, z_n, beta)
    y_n    = IC(a_n, z_n; T)
    (c_n, chi_n) = C_TR(z_n, y_n)
    z_(n+1) = U(z_n, c_n, chi_n)
    j_(n+1) = Sync_J(j_n, Delta z_n) when S_J(Delta z_n)=1

Composition:

    W_beta
      = Sync_J
        o U
        o C_TR
        o IC
        o A
        o G
        o Freeze
        o Phi
        o O_beta

Guarded recursion:

    (z_(n+1), j_(n+1)) = W_beta(z_n, j_n)

Relative closure requires all of:

    chi_n = CLOSED
    R_Q(z_(n+1)) ==_Q R_Q(z_n)
    no live reentry trigger

OPEN and BLOCKED are preserved terminal non-closure states.

## Implementation disposition

Candidate branch: ic-math-first-wrapper-037

Added:
- runtime/math_first_selector.py
- runtime/jane_relevance.py
- runtime/math_first_wrapper.py
- tests/test_math_first_selector.py
- tests/test_math_first_wrapper.py
- tests/test_improvement_core_math_first.py
- tests/test_math_first_wrapper_holdout.py

Narrow compatibility patches:
- runtime/hf_controller.py
  - delta_reentry_route is now the explicit name;
  - trc remains an alias;
  - optional package_selector input.
- runtime/improvement_core.py
  - optional package_selector;
  - default behavior unchanged.

Promotion rule:
Do not replace the current wrapper until the complete branch validation passes and the branch demonstrates preservation plus strict gain.
