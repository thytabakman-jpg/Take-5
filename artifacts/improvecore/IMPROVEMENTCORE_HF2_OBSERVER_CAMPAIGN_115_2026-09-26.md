# ImprovementCore + HF2 Observer Campaign 115

Date: 2026-09-26
Basis: Take-5 483db2a529e11c15d3e03476c81699d9a6c3d1cc
Controller: ImprovementCore / IC-028
HF2: explicit experimental campaign composition
Mode: OBSERVE_DECOUPLED
Mutation: forbidden in this stage

## Input evidence

The whole current conversation plus the current Take-5 system, including:

- full configured MT audit 004;
- validated Protected Transition Integrity;
- post-PTI root recheck;
- Show-Me-the-Math portability distinctions;
- ToolConductor coverage/portability distinctions;
- ICC128 Legacy campaign execution-truth gap;
- ImprovementCore evidence-only runs 113 and 114;
- current HF2 recovery anchor.

## Why HF2 is explicit rather than silently assumed

HF002 is a generic same-capability recursive continuation engine.

Current promoted validated binding:

HF2[RootCause].

Universal HF2 wrapper promotion remains OPEN.

Therefore this campaign uses a typed explicit composition:

HF2[ImprovementCore]_campaign

without claiming that HF2 has already been promoted as a universal ImprovementCore wrapper.

## Observer recurrence

### Round 0

ImprovementCore finding:

PTI_ALREADY_CLOSES_CONFIGURED_TOOL_PATH.

Material distinction:

The recurrent execution-truth problem cannot be treated as an unqualified failure of the
configured-tool path because validated PTI already guards:

identity
-> dispatch
-> execution
-> consumption
-> state update
-> reentry
-> user-visible boundary.

HF2 disposition:

REAPPLY_C.

Reason:

material representation change plus live local frontier.

### Round 1

ImprovementCore finding:

NON_CONFIGURED_EXECUTION_CLAIM_SEAM.

Material distinction:

The live repository-owned recurrence is concentrated in artifacts that can make execution
claims outside the configured-tool PTI path, especially campaign and Legacy reporting.

External boundary separated:

UNIVERSAL_HOST_INTERCEPTION = EXTERNAL_NOT_OWNED.

HF2 disposition:

REAPPLY_C.

Reason:

material relation discovery plus live local frontier.

### Round 2

ImprovementCore finding:

REPOSITORY_OWNED_ARTIFACT_ATTESTATION_GAP.

Observer candidate selected:

EXECUTION_CLAIM_INTEGRITY_GATE.

Constraint:

reuse PTI semantics where applicable;
do not duplicate PTI.

HF2 binding status:

EXPERIMENTAL_CAMPAIGN_BINDING_ONLY.

Mutation performed:

false.

HF2 disposition:

RELATIVE_CLOSE.

## Observer result

Recovered smallest current repository-owned seam:

Repository artifacts that assert execution outcomes can still lack a generic fail-closed
causal attestation gate when they sit outside the configured-tool PTI execution path.

The observer pass does not authorize the implementation.

It supplies the candidate to the next ImprovementCore + HF2 action campaign.

## External verification

Regression:

tests/test_improvecore_hf2_observer_campaign_20260926.py

Expected verified properties:

- current ImprovementCore regime executes each observer round;
- HF2 reapplies the same ImprovementCore capability exactly when material/local/live;
- three rounds occur;
- first two rounds REAPPLY_C;
- final round RELATIVE_CLOSE;
- no mutation occurs;
- PTI is preserved rather than duplicated;
- universal host interception remains outside repository ownership.

## External verification

Take-5 Validation:
36266749501 SUCCESS.

Capability Preservation:
36266749515 SUCCESS.

Observer ImprovementCore + HF2 disposition:

RELATIVE_CLOSE.

Mutation performed:

false.

Observer candidate passed forward:

EXECUTION_CLAIM_INTEGRITY_GATE.
