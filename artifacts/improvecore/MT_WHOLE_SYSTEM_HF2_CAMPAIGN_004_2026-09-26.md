# Full Configured MT Audit 004 — Whole Conversation and Current System

Date: 2026-09-26
Basis: Take-5 main 483db2a529e11c15d3e03476c81699d9a6c3d1cc
Configured tool: MT
Mode: OBSERVER
Wrapper: canonical math-first full wrapper
Geometry: D36_C = 6 scopes x 6 mode faces = 36 cells
Question projections: 22 x 36 = 792
Cognitive projections: 4 x 36 = 144
Black-box return gate: required
Closure/reentry: required

## Target

The whole current conversation plus the current Take-5 system state, with emphasis on
the recurring failures around tool execution, Show-Me-the-Math portability, ToolConductor
coverage, ICC128 Legacy campaign reporting, and ImprovementCore evidence handling.

## 36-cell quotient

The full surface collapses to seven material distinctions.

### 1. CONFIGURED_PLAN != ACTUAL_EXECUTION

A 36-cell plan proves configured reachability. It does not itself prove native or
host-bound semantic execution.

### 2. PTI_INTERNAL_CLOSURE != UNIVERSAL_EXECUTION_TRUTH_CLOSURE

Take-5 already has validated Protected Transition Integrity for repository-governed
configured-tool execution:

identity
-> dispatch
-> execution
-> consumption
-> state update
-> reentry
-> user-visible boundary.

The post-repair root recheck explicitly closes the internal configured-tool PTI generator
relative to the repository path.

Therefore inventing a second configured-tool receipt architecture would duplicate a
validated repair.

### 3. CONFIGURED_TOOL_PATH != CAMPAIGN_OR_LEGACY_ARTIFACT_PATH

The live execution-truth recurrence now appears outside the configured-tool PTI path.

The strongest current example is the prior ICC128 Legacy campaign artifact:

- a portable Legacy self-test executed;
- a campaign report existed;
- GitHub persistence existed;
- but the causal chain from the campaign-specific Controller.run to the report and commit
  receipt was not directly witnessed.

### 4. REPORT_PRESENCE != CAUSAL_EXECUTION_ATTESTATION

A report can accurately describe a run only when its claimed execution coordinates are
bound to runtime evidence.

Persistence alone cannot supply the missing runtime causal edge.

### 5. HF2_GENERIC_ENGINE != PROMOTED_HF2[IMPROVEMENTCORE]

HF002 is a generic same-capability recurrence engine.

Current validated promoted binding:
HF2[RootCause].

Universal HF2 wrapper promotion remains OPEN.

Therefore this campaign may test an explicit HF2[ImprovementCore] composition, but must
label it as an experimental campaign binding until validated.

### 6. REPOSITORY_OWNED_SEAM != UNIVERSAL_HOST_BYPASS

The validated post-PTI root recheck identifies HOST_INTEGRATION_BYPASS as the residual
outside the repository-governed path.

Take-5 cannot force an unrelated host that never invokes Take-5.

That boundary remains EXTERNAL_NOT_OWNED.

A repository repair may still close execution-claim integrity for repository artifacts
that sit outside configured-tool PTI.

### 7. THINK_BIG != DUPLICATE_EXISTING_ARCHITECTURE

The high-leverage candidate is not another general configured-tool receipt mechanism.

The live candidate is:

NON_CONFIGURED_EXECUTION_CLAIM_INTEGRITY

for campaign, Legacy, and other repository artifacts that assert execution facts outside
the configured-tool PTI transition.

## Material black boxes

1. HF2_IMPROVEMENTCORE_BINDING
2. NON_CONFIGURED_EXECUTION_CLAIM_INTEGRITY
3. LEGACY_CAMPAIGN_RUNTIME_PROVENANCE

These enter the configured semantic-resolution discipline.

## MT disposition

MATERIAL_YIELD.

The next licensed stage is observer-first ImprovementCore with an explicit experimental
HF2 composition.

MT does not authorize a repair. It supplies evidence and distinctions.
