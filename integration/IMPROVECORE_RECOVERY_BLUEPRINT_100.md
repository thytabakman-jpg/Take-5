# ImproveCore Recovery Blueprint 100

Date: 2026-09-26
Status: RECOVERY MAP / CURRENT-BASIS CANDIDATE

## Why this exists

ImproveCore has repeatedly become stronger and later appeared weaker because its gains were
distributed across history, runtime, semantic candidates, wrappers, and chat context.

Recovery must not depend on remembering the sequence of conversations.

## What ImproveCore became

The current recovered improvement path is cumulative.

### 1. Controller ownership

The host does not author the internal tool sequence after governed entry.

ImproveCore owns endogenous continuation and typed stopping.

### 2. Observer-governed entry

Whole-object observation can precede goal pressure when representation contamination is a
material risk.

Material representation change triggers renewed observation.

### 3. Inquiry before tool choice

Questions/discriminators and work are generated from unresolved state before capability
selection.

Capability names do not define the problem.

### 4. Realization truth

Selected is not executed.
Executed is not admitted.
Admitted is not persisted.
Persisted is not consumed/reentered.

The chain remains typed.

### 5. Parent-owned recursion

Child ImproveCore runs return evidence.
The parent admits/rejects/reconciles the result, updates state, and owns recurrence.

### 6. Learning memory

NO_GAIN, REJECTED, and FAILED routes survive relative to their basis and dependency
footprint.

Relevant basis change can reopen them.

### 7. Augmented controller mathematics

Learning memory and recursive child-action identity are part of controller state/action
semantics rather than external notes.

### 8. Upstream discovery

The strongest lineage permits discovery to alter the candidate job, relation, and object
universe before ordinary local routing. Runtime promotion remains separately evidenced.

### 9. Take-Two kernel preservation

Sparse semantic primitives, transition-controlled mutation, OPEN preservation, observation,
and views-before-surfaces constrain the controller from becoming architectural bloat.

### 10. Recovery and concurrency

Currentness is recoverable from explicit anchors and manifests.
Concurrent changes cannot be treated as last-writer-wins.

## Recovery order after apparent loss

1. resolve integration/CURRENT_IMPROVEMENT_CORE.md;
2. resolve architecture/IMPROVEMENT_CORE_RECOVERY_MANIFEST_082.json;
3. run the machine recovery validator;
4. load this blueprint;
5. run capability preservation against the declared protected corpus;
6. reconstruct Jane continuity packet;
7. load Take Two Operating Law 099;
8. compare any newer candidate against protected behaviors and capability witnesses;
9. preserve OPEN coordinates;
10. only then permit promotion/currentness mutation.

## Lost-capability rule

When a historical capability reappears, do not merely append it to a list.

Give it:

identity
semantics
reachability
selection
execution
effect/admission
consumer/reentry
recovery witness.

Until those coordinates are dispositioned, capability recovery remains OPEN.

## What this blueprint does not claim

It does not prove the full historical corpus has been exhausted.

It makes future loss detectable and makes the strongest recovered architecture reconstructible
without relying on chat memory.
